# Examples

Examples are small MNCS source fixtures used to explore the framework vocabulary. They are not a stable public API and may need revision as MNCS source profiles evolve.

`first-layout.mncs` is intentionally modest: it constructs a typed rectangle from terminal dimensions. More complete layout and event examples will be added as the language gains the required collections, constraints, effects, and bounded transition forms.
